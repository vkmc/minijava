class A {
	dynamic boolean methodb1 (Objeto o1, Objeto o2)
		var String s1, s2;
		var boolean a, b;
		var int c, d;
	{
		a = true;
		(!a);
		(1+1);
		("una cadena");
		(a || b);
		(a && b);
		(c > d && d < c);
		(c < d || d > c);
	}
}

class Objeto {
	static void main()
	{
	}
}
